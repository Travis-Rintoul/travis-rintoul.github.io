// Please see documentation at https://docs.microsoft.com/aspnet/core/client-side/bundling-and-minification
// for details on configuring this project to bundle and minify static web assets.

// Write your JavaScript code.
$(document).ready(function () 
{
    /*
    let canvas = ParticleMap("#landing-canvas");
    canvas.ctx = canvas.$.get(0).getContext('2d');
    
    canvas.resize();
    
    canvas.width = $(window).width();
    canvas.height = $(window).height();
    
    let particleLimit = (100 * window.devicePixelRatio);

    for (let i=0; i < 100; i++)
    {
        canvas.addParticle(
            U.rand(0, canvas.width),
            U.rand(0, canvas.height)
        );
    }
    
    canvas.init();
    console.log(canvas.particles);
    
    $(window).on('wheel', function (e) 
    {
        slider.onWheel(e);
    });

    $('#page').on('flick', function(e) 
    {
        if (e.direction < 0)
        {
            slider.next();
        }
        else 
        {
            slider.prev();
        }
    });
    
    $("#welcome-contact button").click(function () 
    {
        slider.activeSection = 'contact';    
    });
    */

});

/*
window.onload = function() {
    slider.onLoad();
};
*/
